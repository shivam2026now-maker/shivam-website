import type { Logger } from '../../utils/logger.js';
import type { AssetResource, AuthParams, InstallerType } from '../../utils/types.js';
type ProcessAssetsOptions = {
    assetResources: AssetResource[];
    auth: AuthParams;
    blueprintFileDir: string;
    installer: InstallerType;
    logger: Logger;
    onProgress?: (progress: ProcessAssetsProgress) => void;
    onResult?: (result: ProcessedAsset) => void;
};
export type ProcessAssetsProgress = {
    completed: number;
    total: number;
    slow: boolean;
};
export type ProcessedAsset = {
    success: false;
    resource: AssetResource;
    error: string;
    failureKind: 'processing' | 'upload';
} | {
    success: true;
    resource: AssetResource;
    hash?: string;
    exists?: boolean;
};
export declare function processAssets(options: ProcessAssetsOptions): Promise<ProcessedAsset[]>;
export {};
