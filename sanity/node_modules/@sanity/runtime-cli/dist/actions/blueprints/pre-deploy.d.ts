import type { BlueprintResource } from '@sanity/blueprints';
import type { Logger } from '../../utils/logger.js';
import { type AuthParams, type InstallerType, type ScopeType } from '../../utils/types.js';
import { type ProcessAssetsProgress, type ProcessedAsset } from './process-assets.js';
type PreDeployResult = {
    success: false;
    error: string;
    code?: string;
    exitCode?: number;
    suggestions?: string[];
} | {
    success: true;
};
export type PreDeployAssetEvent = {
    type: 'progress';
    progress: ProcessAssetsProgress;
} | {
    type: 'complete';
} | {
    type: 'result';
    result: ProcessedAsset;
};
type PreDeployOptions = {
    scopeType: ScopeType;
    scopeId: string;
    resources: BlueprintResource[];
    stackId: string;
    stackName: string;
    bin: string;
    verbose: boolean | undefined;
    auth: AuthParams;
    blueprintFileDir: string;
    installer: InstallerType;
    logger: Logger;
    onAssetEvent?: (event: PreDeployAssetEvent) => void;
};
export declare function preDeploy(options: PreDeployOptions): Promise<PreDeployResult>;
export {};
