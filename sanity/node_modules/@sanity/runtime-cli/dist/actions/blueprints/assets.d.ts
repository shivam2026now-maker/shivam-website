import AdmZip from 'adm-zip';
import type { Logger } from '../../utils/logger.js';
import type { AssetResource, AuthParams, InstallerType } from '../../utils/types.js';
export declare const assetCheckUrl: string;
export declare const assetStashUrl: string;
export declare function stashAsset({ resource, auth, blueprintFileDir, logger, installer, }: {
    resource: AssetResource;
    auth: AuthParams;
    blueprintFileDir: string;
    logger: Logger;
    installer?: InstallerType;
}): Promise<{
    success: boolean;
    assetId?: string;
    exists?: boolean;
    hash?: string;
    error?: string;
}>;
export declare function hashBuffer(buffer: Buffer): string;
export declare function pathToZip(path: string): Promise<AdmZip>;
export declare function pathToB64ZipHash(path: string): Promise<{
    b64: string;
    buffer: Buffer;
    hash: string;
    zip: AdmZip;
}>;
